# Whapi / WhatsApp API
WHAPI_TOKEN=tu_token_whapi

# OpenAI (para parafrasear el texto de las alertas)
OPENAI_API_KEY=sk-...
WHAPI_GROUP_ID=120363423858096188@g.us

# GitHub (opcional): para que el droplet suba web/alertas.json y la página se actualice
# Crear en: GitHub → Settings → Developer settings → Personal access tokens
# Permisos: repo (acceso completo)
GITHUB_TOKEN=tu_token_github
